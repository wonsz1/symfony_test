{% extends 'base.html.twig' %}
{% block title %}Posty{% endblock %}
{% block body %}
<h1>Posty</h1>
<ul>
    {% for post in posts %}
        <li><a href="{{ path('post_show', {'slug': post.slug}) }}">{{ post.title }}</a> ({{ post.category.name }})</li>
    {% else %}
        <li>Brak postów.</li>
    {% endfor %}
</ul>
<a href="{{ path('post_new') }}" class="btn btn-primary">Dodaj nowy post</a>
{% endblock %}
