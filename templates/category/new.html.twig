{% extends 'base.html.twig' %}
{% block title %}Add Category{% endblock %}
{% block body %}
<h1>Add Category</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Save</button>
{{ form_end(form) }}
{% endblock %}
